# 第 1 天 · 拉数据、算收益、画图（2 小时）

前提：`day0.md` 的环境和市场选择已完成。

目标：对一只标的跑通「下载日线 → 看懂 OHLCV → 算收益与回撤 → 画图」。不写交易规则。

---

### A. 取日线并落盘（40 分钟）

选一只你认识的标的（沪深 300、贵州茅台、或 SPY），最近 2 年日线。

- [ ] 用 `akshare` 或 `yfinance` 拉 OHLCV，列名统一成 `open/high/low/close/volume`，索引为交易日
- [ ] 确认是否前复权；A 股没有复权的价格序列不能直接算均线
- [ ] 检查空洞：停牌、节假日、接口丢行。先打印 `head/tail/describe` 和缺失值数量
- [ ] 存到 `data/`（parquet 或 csv），后面几天不要反复打接口

**卡点：** 接口报错、列名是中文、时区把日期弄成 UTC。今天只要求「能画出收盘价」，不要写通用数据平台。

### B. 收益率，不是价格（40 分钟）

- [ ] 日收益率：`ret = close.pct_change()`
- [ ] 累计收益：`(1 + ret).cumprod() - 1`
- [ ] 最大回撤：累计净值相对历史峰值的最大跌幅
- [ ] 记下三个数：区间总收益、最大回撤、日收益标准差（年化可先用 `std * sqrt(252)`，知道它是近似即可）

价格高的股票和价格低的股票不能比绝对值；收益和回撤才是后面策略的对照基线（买入持有）。

**买入持有基线 = 一笔买入 + 一笔卖出。** 期初全仓买入、一直持有到期末，没有信号、没有再平衡、不扣手续费。把它的累计净值和最大回撤算出来，后面任何策略都必须先打过这条线才值得做：

````python
close = df["close"]
ret = close.pct_change()                 # 日收益率
nav = (1 + ret).cumprod()                # 累计净值（期初 = 1）
total_return = nav.iloc[-1] - 1          # 区间总收益

peak = nav.cummax()                      # 滚动峰值
drawdown = nav / peak - 1                # 始终 <= 0
max_drawdown = drawdown.min()            # 最大回撤（负数）

daily_vol = ret.std()
annual_vol = daily_vol * (252 ** 0.5)    # 年化波动率（近似）

print(f"区间总收益: {total_return:.2%}")
print(f"最大回撤:   {max_drawdown:.2%}")
print(f"年化波动率: {annual_vol:.2%}")
````

### C. 画两张图（30 分钟）

- [ ] 收盘价
- [ ] 累计收益曲线
- [ ] 图上标出最大回撤发生的大致区间（目视即可）
- [ ] 保存到 `notebooks/` 或 `data/` 旁，后面对比策略时要拿来叠

### D. 收工（10 分钟）

能用自己的代码回答：

1. 这根序列从哪天到哪天、缺不缺数据？（打印 `df.index[0] ~ df.index[-1]`，以及 `df.isna().sum()` 和 B 节里「自然日 / 根数」比例）
2. 买入持有的总收益和最大回撤是多少？（直接读 B 节打印的 `total_return`、`max_drawdown`）
3. 明天的规则如果还跑不赢这根基线，说明什么？（答：策略在扣除交易成本前就已经输给被动持有，再考虑手续费/滑点就更没意义——要么规则无效，要么样本期对这个标的不适用，换标的或换周期再试）

**不要做：** 均线策略、回测框架、多标的、分钟线。
