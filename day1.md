# 第 1 天 · 拉数据、算收益、画图（2 小时）

前提：`day0.md` 的环境和市场选择已完成。

目标：对一只标的跑通「下载日线 → 看懂 OHLCV → 算收益与回撤 → 画图」。不写交易规则。

---

### A. 取日线并落盘（40 分钟）

选一只你认识的标的（沪深 300、贵州茅台、或 SPY），最近 2 年日线。两年是作业量，不是「策略必须两年」：你喂多少行，后面的循环就跑多少次。均线还要预热（见 `day2.md`）；回测几年才可信见 `day6.md`。

**先看懂一根 K 线。** 它不是软件里那条起伏的「曲线」——那是很多根并排。一根 = 一个时间桶的 OHLCV。日频 = 一天一根；5 分钟线 = 每 5 分钟一根。国内软件通常红涨绿跌。图上看 **空心=阳线（收>开）**，**实心=阴线（收<开）**。

```
例1 阳线 = 空心（收 106 > 开 100，红涨）
开 100  高 108  低 99  收 106

    108   |  ---- 上影（最高）
          |
    106 +---+  收（实体上沿）
        |   |
        |   |  空心：今天收在开盘上方
        |   |
    100 +---+  开（实体下沿）
          |
     99   |  ---- 下影（最低）


例2 阴线 = 实心（收 100 < 开 106，绿跌）
开 106  高 107  低 98  收 100

    107   |  ---- 上影（最高）
          |
    106 #####  开（实体上沿）
        #####
        #####  实心：今天收在开盘下方
        #####
    100 #####  收（实体下沿）
          |
     98   |  ---- 下影（最低）
```

约束：最低 ≤ 开、收 ≤ 最高。影线可以没有（开或收碰巧就是最高/最低）。

akshare 不用 token。`period="daily"` 才是日线；`adjust="qfq"` 前复权（A 股不复权不能直接算均线）。

```python
import akshare as ak

df = ak.stock_zh_a_hist(
    symbol="600519",          # 6 位代码，不要 sz/sh 前缀
    period="daily",
    start_date="20240801",
    end_date="20260801",
    adjust="qfq",             # 前复权；"" 不复权，"hfq" 后复权
)
print(df.head())
print(df.columns)             # 中文列名：日期/开盘/收盘/最高/最低/成交量
```

完整落盘脚本是 `day1_a.py`。

- [ ] 用 `akshare` 或 `yfinance` 拉 OHLCV，列名统一成 `open/high/low/close/volume`，索引为交易日
- [ ] 确认是否前复权
- [ ] 检查空洞：停牌、节假日、接口丢行。先打印 `head/tail/describe` 和缺失值数量
- [ ] 存到 `data/`（parquet 或 csv），后面几天不要反复打接口

**卡点：** 接口报错、列名是中文、时区把日期弄成 UTC。今天只要求「能画出收盘价」，不要写通用数据平台。

### B. 收益率，不是价格（40 分钟）

- [ ] 日收益率：`ret = close.pct_change()`（pandas 的 `Series` 方法，不是另装的库。 表示「相对上一行变了百分之多少」，等价于 `close / close.shift(1) - 1`）
- [ ] 累计收益：`(1 + ret).cumprod() - 1`
- [ ] 最大回撤：累计净值相对历史峰值的最大跌幅
- [ ] 记下三个数：区间总收益、最大回撤、日收益标准差（年化可先用 `std * sqrt(252)`，知道它是近似即可）

价格高的股票和价格低的股票不能比绝对值；收益和回撤才是后面策略的对照基线（买入持有）。

**买入持有基线 = 一笔买入 + 一笔卖出。** 期初全仓买入、一直持有到期末（所以一定横跨很多天），中间没有信号、没有再平衡、不扣手续费。

**有什么用：** 它是「什么都不做、只当股东」的成绩单。后面任何均线、择时规则，都要先和它比，而不是和 0 比。策略多了手续费和空仓期，若总收益还不如一直拿着、回撤也没明显变小，这套规则就没有存在的必要——你本来可以直接买了放到期末。它不是交易策略，是对照组。

**定投算不算量化？** 算一种很简单的量化（系统化投资），但不是第 2 天那种择时。

- 规则可重复：例如每月第一个交易日用固定金额买，不看均线、不预测涨跌。
- 和买入持有的差别：基线是 **期初一笔买完**；定投是 **把同一笔钱拆成很多次买**。信号来自日历，不是价格。
- 用途主要是：工资分期入场、避免纠结「哪天 All-in」。在长期上涨的市场里，定投的终点资金往往 **不如** 期初一次性买入持有（钱越晚进场，享受上涨的时间越短）。

**为什么均线不先和定投比？** 可以比，但对照必须和你的资金约束一致，否则在比两件不同的事。

- 钱 **期初就已经在账户里**：公平对照是一次性买入持有。均线空仓时现金在吃利息（本课程先当 0），定投却是人为把钱留在外面、分批进场。打赢定投太容易——因为对手故意晚买，不是因为均线有择时能力。
- 钱 **每个月才发下来**：这时一次性买入持有做不到，公平对照才是定投。要比的是「每月固定买」vs「每月金额相同，但均线说买才买、否则现金等到下月」。两边总投入节奏要一样。
- 本课程前几天假设你已经有一笔钱在做日频规则，所以默认对照是买入持有。以后若模拟工资入金，再加一条定投基线，不要互相替代。

成交价用的是 **收盘价，不是开盘价，也不是最高/最低价**。`close.pct_change()` 是「今天收盘相对昨天收盘」：在第一根 K 线的收盘价买入，在最后一根 K 线的收盘价卖出。第一天开盘到收盘的涨跌不计入（那天的 `pct_change` 是 NaN）。后面任何策略都必须先打过这条线才值得做：

````python
close = df["close"]
ret = close.pct_change()                 # 日收益率
nav = (1 + ret).cumprod()                # 累计净值（期初 = 1）
total_return = nav.iloc[-1] - 1          # 区间总收益

peak = nav.cummax()                      # 滚动峰值
drawdown = nav / peak - 1                # 始终 <= 0
max_drawdown = drawdown.min()            # 最大回撤（负数）

daily_vol = ret.std()
annual_vol = daily_vol * (252 ** 0.5)    # 年化波动率（近似）

print(f"区间总收益: {total_return:.2%}")
print(f"最大回撤:   {max_drawdown:.2%}")
print(f"年化波动率: {annual_vol:.2%}")
````

### C. 画两张图（30 分钟）

- [ ] 收盘价
- [ ] 累计收益曲线
- [ ] 图上标出最大回撤发生的大致区间（目视即可）
- [ ] 保存到 `notebooks/` 或 `data/` 旁，后面对比策略时要拿来叠

### D. 收工（10 分钟）

能用自己的代码回答：

1. 这根序列从哪天到哪天、缺不缺数据？（打印 `df.index[0] ~ df.index[-1]`，以及 `df.isna().sum()` 和 B 节里「自然日 / 根数」比例）
2. 买入持有的总收益和最大回撤是多少？（直接读 B 节打印的 `total_return`、`max_drawdown`）
3. 明天的规则如果还跑不赢这根基线，说明什么？（答：策略在扣除交易成本前就已经输给被动持有，再考虑手续费/滑点就更没意义——要么规则无效，要么样本期对这个标的不适用，换标的或换周期再试）

**不要做：** 均线策略、回测框架、多标的、分钟线。
